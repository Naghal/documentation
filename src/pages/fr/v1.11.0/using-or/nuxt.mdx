---
Titre: "Utiliser OpenReplay avec Nuxt"
metaTitle: "Ajouter le suiveur OpenReplay dans votre application Nuxt.js"
metaDescription: "Apprenez à faire fonctionner le suiveur sur votre application Nuxt"
---
import GoDeeperFooter from '~/components/go-deeper-footer.astro'

Il est assez simple d'obtenir le suiveur pour fonctionner sur une application Nuxt.

## Ajouter le code de suivi

Bien que vous puissiez obtenir une version copier-coller du code directement à partir de la plate-forme lorsque vous créez un nouveau projet, ce code est trop basique et peut ne pas suivre les meilleures pratiques attendues par votre équipe ou votre entreprise.

Au lieu de cela, vous pouvez essayer de moduler le code fourni de cette façon:


```jsx
import Tracker from '@openreplay/tracker';
import {v4 as uuidV4} from 'uuid'

function defaultGetUserId() {
   return uuidV4() 
}

export function startTracker(config) {

    console.log("Starting tracker...")

    const getUserId = (config?.userIdEnabled && config?.getUserId) ? config.getUserId : defaultGetUserId
    let userId = null;

    const trackerConfig = {
        projectKey: config?.projectKey
    }

    const tracker = new Tracker(trackerConfig);

    if(config?.userIdEnabled) {
        userId = getUserId()
        tracker.setUserID(userId)
    }

    tracker.start();
    return {
        tracker,
        userId
    }
}
```


Cette fonction s'occupe d'instancier le Suiveur et d'appeler la méthode `start`, qui fait fonctionner les choses.

Cette fonction prend également en charge un objet de "configuration" que vous pouvez étendre pour des fonctionnalités futures. Cet exemple vous montre comment activer, en option, la possibilité d'identifier les utilisateurs de manière unique. Vous pouvez utiliser un UUID par défaut qui serait unique pour chaque utilisateur chaque fois qu'ils visitent la page. Vous pouvez également fournir une fonction `getUserId` personnalisée en tant que partie de la configuration.

Avec votre fonction personnalisée, vous pourriez fournir l'ID client de votre magasin, l'adresse e-mail de l'utilisateur ou tout ce qui identifie de manière unique l'utilisateur dans votre application. De cette façon, vous pouvez suivre leurs reprises de session dans le temps au cas où ils auraient un problème particulier que d'autres ne signalent pas.

Dans cet exemple, la clé du projet, que vous pouvez obtenir à partir de l'interface utilisateur du projet OpenReplay, est censée être passée à l'intérieur de l'objet `config` sur la clé `projectKey`.

## Écrire un plugin client uniquement

Pour utiliser cette fonction `startTracker`, vous voudrez créer un plugin client uniquement dans le répertoire `plugins` de votre projet Nuxt.

Assurez-vous d'ajouter le suffixe `.client` au nom de votre fichier de plugin. C'est pour s'assurer que Nuxt ne le charge que sur le client.

Le code du plugin pourrait ressembler à ceci:


```jsx
import {startTracker} from '../utils/tracker'

export default defineNuxtPlugin( (nuxtApp) => {
    
    return {
        provide: {
            startTracking: () => {
                let config = useRuntimeConfig().public
                
                let {userId} = startTracker({
                    userIdEnabled: true,
                    projectKey: config.openreplayProjectKey
                })
                //Optional if you need it
                let uid = useUserId()
                uid.value = userId
            }
        }
    }
})
```


Si vous souhaitez en savoir plus sur la création de plugins Nuxt, vous pouvez consulter [sa documentation](https://v3.nuxtjs.org/guide/directory-structure/plugins).

Maintenant, ce plugin fournit une fonction appelée `startTracking`, qui s'occupe d'appeler la fonction `startTracker` que nous avons définie avant. Mais plus important encore, à l'intérieur du code du plugin, vous pouvez obtenir la variable d'environnement où vous avez stocké la clé du projet. Rappelez-vous, cette information ne doit pas être codée dans votre projet pour des raisons de sécurité.

Grâce à la fonction `useRuntimeConfig`, vous pouvez accéder aux variables d'environnement publiques. Dans cet exemple, j'accède à `openreplayProjectKey` qui est défini comme `NUXT_PUBLIC_OPENREPLAY_PROJECT_KEY`.

💡 **Remarque** le préfixe `NUXT_PUBLIC`, cela permet à Nuxt de partager la variable avec le front-end; sinon, elle ne sera accessible qu'à votre code back-end uniquement.

L'exemple ci-dessus vous montre également comment utiliser la valeur d'ID d'utilisateur retournée avec un composable pour partager la valeur entre les composants. Ces deux lignes sont entièrement facultatives.

## Utiliser le suiveur

Avec le plugin enregistré dans un fichier, vous pouvez ensuite l'utiliser de cette façon à partir de votre fichier `app.vue`.


```jsx
<script setup>
onMounted( () => {
  const {$startTracking} = useNuxtApp()
  $startTracking()
})
</script>

<template>
  <div>
    <NuxtPage />
  </div>
</template>
```

