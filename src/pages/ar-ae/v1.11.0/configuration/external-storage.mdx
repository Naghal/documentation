['---\nالعنوان: "التخزين الخارجي"\nmetaTitle: "التخزين الخارجي"\nmetaDescription: "كيفية تغيير وجهة التخزين الافتراضية لحجوزاتك."\n---\n\nتتم تسجيلات OpenReplay [session recordings](/configuration/cleanup-storage#recordingstorage) بشكل افتراضي في المثيل الخاص بك ، ولكن يمكن تغيير الوجهة إلى أي خدمة تخزين الكائنات المتوافقة مع S3 ، مما يسمح بالتوسع الأكبر وتكاليف التثبيت الأقل.\n\n## تغيير وجهة تخزين التسجيلات\n\nيمكن تخزين التسجيلات في أي وجهة تخزين الكائنات طالما كانت متوافقة مع S3 ، مما يجعل OpenReplay متوافقًا مع جميع الخدمات السحابية الرئيسية. أدناه خطوات لتكوين وجهة مختلفة لتسجيلاتك:\n\n### إنشاء الحزم اللازمة\n\nيجب عليك إنشاء الحزم أدناه في خدمة تخزين الكائنات الخاصة بك. يمكن أن تكون أسماؤها مختلفة ، ولكن سنستخدم المراجع أدناه في بقية هذا القسم:\n- `openreplay-recordings`: حيث سيتم تخزين التسجيلات\n- `openreplay-assets`: حيث سيتم نسخ الأصول (مثل css والخطوط)\n- `openreplay-sourcemaps`: لتخزين الخرائط المصدر (انظر [كيفية تحميلها](/installation/upload-sourcemaps))\n\nبمجرد إنشاء الحزمة:\n\n1. قم بتمكين CORS لكل من الحزم المذكورة أعلاه. إذا كنت في AWS ، فسيكون ذلك تحت 'الأذونات> مشاركة مصادر العرض المتعدد (CORS)' باستخدام التكوين التالي:\n\n
```json
[
    {
        "AllowedHeaders": [
            "*"
        ],
        "AllowedMethods": [
            "GET"
        ],
        "AllowedOrigins": [
            "*"
        ],
        "ExposeHeaders": []
    }
]
```
', "\n\n2. قم بتمكين الوصول العام إلى حزمة `openreplay-assets` **فقط**. هذه الأخيرة تحتوي فقط على الأنماط والأيقونات والخطوط التي تلزم لإعادة تشغيل الجلسة الصحيحة. إذا كنت في AWS ، فانتقل أولا إلى 'الأذونات> منع الوصول العام (إعدادات الحزمة)' وأوقفه ، ثم انتقل إلى 'الأذونات> سياسة الحزمة' واستخدم التكوين التالي:\n\n
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "AddPerm",
            "Effect": "Allow",
            "Principal": "*",
            "Action": "s3:GetObject",
            "Resource": "arn:aws:s3:::openreplay-assets/*"
        }
    ]
}
```
", '\n\n3. وأخيرا ، تأكد من إنشاء مفاتيح الوصول المناسبة إلى **هذه الحزمتين فقط** حتى يمكن للخلفية OpenReplay الوصول بشكل برمجي إليها لتحميل الملفات. الأذونات S3 المطلوبة هي:\n\n
```json
"s3:PutObject",
"s3:GetObjectAcl",
"s3:GetObject",
"s3:GetObjectTagging",
"s3:ListBucket",
"s3:PutObjectTagging",
"s3:GetBucketLocation"
```
']
